Closes #
<!-- Fast-path PR (docs/config only, no issue)? Delete the line above and
instead give the one-sentence summary below. -->

## Summary of changes

<!-- What changed and why, in a few sentences. Fast-path PRs: one sentence
covers it — if it doesn't, this isn't a fast-path change; open an issue. -->

## Testing

<!-- How this was verified: commands run and their results (e.g. `uv run pytest`,
`uv run ruff check .`), plus anything a reviewer must check manually. -->

## Docs

<!-- Tick each, or say why it doesn't apply. An honest "no shape change" beats a
rubber-stamped tick. -->

- [ ] `ARCHITECTURE.md` updated — or: no change to the system's shape
- [ ] `STATE.md` reflects reality after this merge
- [ ] ADR added under `docs/decisions/` and indexed — required only if this change
      constrains future work and would be expensive to reverse; otherwise, no ADR needed

## Before merging

<!-- Commits pushed after this description was written are the common case, not
the exception. Summarize them in a comment rather than editing this box. -->

- [ ] `gh pr view <N> --json commits` checked against this description and the
      comments below — every commit is described somewhere a reader can find
